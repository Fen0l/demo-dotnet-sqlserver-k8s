apiVersion: v1
kind: Namespace
metadata:
  name: sqlprobe
  labels:
    app.kubernetes.io/part-of: dotnet-sqlserver-poc
    # Pod Security Admission: the image runs non-root, read-only rootfs, no capabilities.
    pod-security.kubernetes.io/enforce: restricted
